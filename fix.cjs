const Jimp=require('jimp');Jimp.read('public/croatia_heart_map.png').then(img=>{const w=img.bitmap.width;const h=img.bitmap.height;const bgC=img.getPixelColor(10,10);for(let y=Math.floor(h*0.75);y<h;y++){for(let x=Math.floor(w*0.5);x<w;x++){const p=img.getPixelColor(x,y);const r=Jimp.intToRGBA(p);if(r.r>160&&r.g>140&&r.b<130){img.setPixelColor(bgC,x,y);}}}img.write('public/croatia_heart_map_fixed.png');console.log('Fixed');}).catch(console.error);
